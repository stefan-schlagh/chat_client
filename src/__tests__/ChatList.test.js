import React, { Component } from 'react';
import ChatList from "../Home/allChats/ChatList";
import { shallow } from 'enzyme';

describe('ChatList',() => {
    it("render ChatList",async () => {
       //const chatList = await shallow(<ChatList />)
        expect(1).toBe(1);
    });
});